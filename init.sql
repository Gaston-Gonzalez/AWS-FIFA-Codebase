CREATE TABLE fifa23 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);

CREATE TABLE fifa22 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);

CREATE TABLE fifa21 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);

CREATE TABLE fifa20 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);

CREATE TABLE fifa19 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);

CREATE TABLE fifa18 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);

CREATE TABLE fifa17 (
    id BIGINT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    nationality VARCHAR(50),
    overall INT,
    club VARCHAR(50),
    value BIGINT,
    position VARCHAR(20)
);
